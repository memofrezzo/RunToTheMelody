using UnityEngine;

public class AnimationEventReceiver : MonoBehaviour
{
    // Método que recibirá el evento OnFootstep.
    public void OnFootstep(AnimationEvent animationEvent)
    {
        // Deja este método vacío para que no se dispare ningún error.
    }

    // Método que recibirá el evento OnLand.
    public void OnLand(AnimationEvent animationEvent)
    {
        // Este método también se deja vacío.
    }
}